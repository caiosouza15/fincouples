// Dashboard antigo, com a lógica de negócio real (contexts, cálculos de
// saldo, faturas, metas). Mantido como referência para quando o novo
// layout visual (Dashboard.tsx) for religado aos dados reais.
export { default as DashboardLegacy } from './DashboardLegacy';
